# NETTRADE — Build State (resume here)

**App:** Tauri + React 19 + Vite 7 + Tailwind (HashRouter). Dev server: `cd ~/nettrade && npm run dev` → http://localhost:1420. Design mockups served for verification: `cd ~/nettrade/prototypes && python3 -m http.server 8181`.

**Design language (LOCKED): "CREAM & PIGMENT"** — see `docs/DESIGN_PRINCIPLE.md` + `.claude/skills/comic-collage-ui/`. Cream/ink base; per-screen ACCENT colour; consistent parallelogram look via **clip-path** (NOT skewX — skewX overhangs/clips); bold ink outlines; hard zero-blur offset shadows; halftone; subtle **CRT** frame on every screen; data shown as gauges/charts that **click-to-expand → clear numbers on the right, decoration on the border**; **animated accent liquid-marble** (menu technique, recoloured per screen) on pop-up borders + tasteful accents (outer frame, header tags) — never over data. Data screens stay CLEAN/ALIGNED (parallelograms consistent, not random-wonky). Modals: z-index ABOVE the CRT (#crt z90 → modal z100), NO clip-path on the frame, `overflow:visible`, viewport-level fixed overlay. Verify VISUALLY in a real browser (Chrome viewport is shorter than the OS window).

**Chart/gauge/informatics craft (LOCKED): `docs/CHART_CRAFT.md`** — the data-viz craft layer of CREAM & PIGMENT (built via a Hallmark + ui-ux-pro-max + built-Dashboard multi-agent pass; grounded in the shipped `drawX(c,big)`/`fitCanvas`/`line`/`NtMarble`/`EXP[]` primitives + real tokens). Every chart/gauge/donut/radar/sparkline/P-L/readout follows it: canvas-only (no Recharts/D3/Chart.js), ink-offset zero-blur "stamp" on every mark, halftone-under-cream + marble-on-borders-only, ONE per-screen `--accent` ≤3–5%, gain/loss never colour-only (sign + drawn ▲/▼ + position), tabular figures (Anton has none → live heroes in Space Mono), click-to-expand law (viz left → boxed numbers right, decoration on border, z100), ≤2 motion primitives + reduced-motion snap, honest geometry (true-zero, no invented numbers), 4 in-frame states (empty/loading-skeleton/error+Retry/stale), full a11y (role=img + data-derived aria summary + hidden table mirror). Shared React family to build: ChartFrame · ChartStates · InkAxis · ValueChart · Gauge · Donut · PLBars · Sparkline · Radar · StatReadout · ExpandViz/ExpandModal. **Run the doc's 24-point chart slop-test before shipping any chart.** Open per-screen items (accent hexes for magenta/sky/violet/amber/gold/lime + collision/font-gate/redraw/gesture rules) are listed at the foot of CHART_CRAFT.md — resolve as each screen lands.

**Per-screen accent map:** Main Menu=ORANGE (locked) · Dashboard=TEAL · Positions=MAGENTA · Watchlist=SKY BLUE · Performance=LIME · Compare=VIOLET · Journal=AMBER · Settings=GOLD/AMBER (swappable — one accent var drives the marble).

**Status (2026-06-30): ALL 8 SCREENS BUILT + PORTED + VERIFIED. `npm run build` GREEN. App routable from the menu.**
- **Shared viz extracted** → `src/viz/` (canvas.ts: fitCanvas/line/triCanvas/drawDotCluster; Marble.tsx: NtMarble/MarbleDefs; Crt.tsx+css; ExpandModal.tsx; vizState.ts). Dashboard + Positions retrofitted to import them (behavior-preserving, pixel-verified). New screens import them too.
- Main Menu: LOCKED + PORTED (`CreamMenu.tsx`, route `/`). The menu ITEMS already route to every screen.
- Dashboard (TEAL, route `/dashboard`): PORTED + **chart-craft UPGRADED** (canvas-drawn ▲/▼, 4 viz states, tabular figures, ink-stamp parity) + tightened to Positions density. Verified.
- Positions (MAGENTA, `/positions`): signed off + ported (sortable deck + P/L bars + sparklines + donut/movers/gauge rail + position-detail modal). Verified.
- Watchlist (SKY `#1FA6F0`, `/watchlist`): scanner board (watch cards + candlestick/SMA/EMA/RSI/MACD focal). Ported + verified.
- Performance (LIME `#B4E600`, `/performance`): scrubbable truth-engine timeline + drawdown + XIRR/TWR gauges. Ported + verified. **PORT BUG FIXED:** the hero/draw/nums panels were rendered with only `s.hero`/`s.draw`/`s.nums` (missing the `panel` flex-column class) → `.pBody{flex:1}` inert → absolute canvas collapsed to 0 height → blank charts. Fix: `className={`${s.panel} ${s.hero}`}` on those three sections (gaug intentionally stays `s.gaug`). **LESSON: when porting a mockup whose panels use multiple classes (`class="panel hero"`), carry ALL of them — a dropped layout class silently zero-heights absolute-positioned canvases (no console error).**
- Compare (VIOLET `#7A3CFF`, `/compare`): you-vs-partner duel, mirrored diverging bars, combined-fund strip. Ported + verified.
- Journal (AMBER `#F2A416`, `/journal`): decision log + intent payoff bars + hit-rate gauge. Ported + verified.
- Settings (GOLD, `/settings`): tabbed control panel (Accounts/Data/Appearance/Sound). Ported + verified.
- All ported via a parallel workflow (each screen its own files) + one route-wiring pass; verified by my own headless screenshots of every screen at 1920.
- **Navigation (2026-06-30):** hub-and-spoke. Every data screen has a working **BACK → menu** (an on-brand accent pill added to each bottom status bar) + the **NT logo** (top-left) also returns to the menu. Shared keyboard hook `src/viz/useScreenNav.ts`: **1–7** jump straight to each screen (menu order), **0 / M** → menu; guarded so it never fires while typing in input/select/textarea/contentEditable. Verified live (key `4`→Performance, `M`→menu, BACK pill→menu). Esc still only closes an expanded chart.
- **Flashing/flicker FIXED (2026-06-30):** the whole app flickered (worst on popup open). Root cause = the animated liquid-marble's `feTurbulence`+`feDisplacementMap` filter re-rasterizing every frame (drift was animating a group INSIDE the filter; SMIL morphed the noise). Fix: static turbulence (SMIL removed from `viz/Marble.tsx`) + drift moved to a GPU-composited transform on `.ntMarble > svg` (+ `contain:paint`, `will-change`, reduced-motion repointed) across all 7 data screens AND `CreamMenu.module.css` (its own marble). Marble still drifts. Verified by inter-frame pixel diff: **10.5 → 0.07–0.21** (data screens) and the menu settles to ~0.3 (marble column drift only, rest static). See [[work-efficiency-visual-bugs]] for the diff technique.
- **Real-data phase progress:** **Phase 2 DONE (2026-06-30, builds green, Opus-reviewed, awaiting user live-key test)** — keychain (`keyring`) commands + tauri-plugin-sql/-http + `src/adapters/trading212.ts` reading `/equity/positions` into a plain list in the Settings → API Keys vault. CRT treatment LOCKED; main-menu wordmark redesigned (one-line, Anton-box NET + Nabla TRADE); `.app` rebuilt incl. Phase 2. See `docs/HANDOFF.md` §9 + `docs/PHASE2_TEST.md`. **NEXT = Phase 3** (Performance-Truth engine: data model + CSV backfill + pure Vitest money maths), then Phase 4 wires the screens (still MOCK data today). A global ?state= demo toggle exists on the viz states (dev affordance).

**Process:** design each screen as a standalone HTML mockup in `prototypes/` (fluid full-viewport, Google Fonts, self-contained), verify with headless Chrome / Playwright (file:// blocked → use the :8181 server), get user sign-off, then port to `src/screens/<Name>.tsx` + module CSS and wire the route. Reuse the marble + clip-path + CRT components. Mock data in `src/data/portfolio.ts`. Keep `npm run build` green.
